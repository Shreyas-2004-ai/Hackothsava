# Install Supabase Auth Helpers

Run this command to install the required package for authentication middleware:

```bash
npm install @supabase/auth-helpers-nextjs
```

This package provides:
- Middleware support for route protection
- Session management
- Automatic token refresh
- Server-side authentication helpers

After installing, restart your dev server:

```bash
npm run dev
```
